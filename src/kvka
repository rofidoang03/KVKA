#!/bin/bash

k_1="Selamat datang di KVKA"
k_2="KVKA adalah skrip Bash sederhana yang dibuat untuk mengonversi file video menjadi file audio pada sistem operasi Linux (Debian/Ubuntu) menggunakan perangkat lunak FFmpeg."

echo -n "$k_1" | pv -qL 10
echo -e "\n"
echo -n "$k_2" | pv -qL 10
echo ""

read -p "Masukkan nama file video: " v
read -p "Masukkan nama file audio: " a

ffmpeg -i "${v}" -vn -acodec libmp3lame "${a}"
mv "${a}" /root/video2audio/conversion_results

echo "Konversi selesai."
echo "Hasil konversi disimpan di folder '/root/kvka/hasil_konversi'."
